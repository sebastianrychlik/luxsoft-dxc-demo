# M1.0 — Angular Foundation

## Objective

Establish the enterprise Angular frontend architecture for the Luxsoft DXC Demo project.

This milestone creates only the project structure, infrastructure, and architecture skeleton.
No business functionality is implemented.

---

## Scope

- Angular 18 project scaffolded manually (no `ng new`)
- Standalone component architecture throughout
- Lazy-loading ready routing
- Core infrastructure layer (services, interceptors, guards)
- Shared UI layer (components, pipes, directives)
- Feature and layout folder placeholders
- Environment configuration for local and production
- TypeScript strict mode enabled
- Signals-ready architecture

---

## Technologies

| Technology | Version |
|---|---|
| Angular | 18.x |
| TypeScript | 5.4.x |
| RxJS | 7.8.x |
| Node.js | 20.x |
| npm | 10.x |

---

## Folder Structure

```
frontend/
├── public/                          # Static assets
├── src/
│   ├── app/
│   │   ├── app.component.ts         # Root component (router outlet only)
│   │   ├── app.config.ts            # Application providers configuration
│   │   ├── app.routes.ts            # Root route definitions
│   │   │
│   │   ├── core/                    # Singleton infrastructure (one instance per app)
│   │   │   ├── interceptors/
│   │   │   │   ├── error.interceptor.ts      # Global HTTP error handling
│   │   │   │   └── logging.interceptor.ts    # Request/response logging
│   │   │   ├── guards/
│   │   │   │   └── auth.guard.ts             # Route authentication guard
│   │   │   └── services/
│   │   │       ├── api.service.ts            # Central HTTP wrapper
│   │   │       ├── auth.service.ts           # Authentication state (signals)
│   │   │       └── error-handler.service.ts  # Global Angular error handler
│   │   │
│   │   ├── shared/                  # Reusable UI building blocks
│   │   │   ├── components/
│   │   │   │   ├── not-found/       # 404 page component
│   │   │   │   └── spinner/         # Loading spinner component
│   │   │   ├── directives/
│   │   │   │   └── click-outside.directive.ts
│   │   │   └── pipes/
│   │   │       └── truncate.pipe.ts
│   │   │
│   │   ├── features/                # Business feature modules (lazy-loaded)
│   │   │   └── dashboard/           # Dashboard placeholder
│   │   │
│   │   └── layouts/                 # Application layout wrappers
│   │       └── main-layout/         # Primary layout shell placeholder
│   │
│   ├── environments/
│   │   ├── environment.ts           # Development environment config
│   │   └── environment.production.ts # Production environment config
│   │
│   ├── index.html                   # Application HTML entry point
│   ├── main.ts                      # Bootstrap entry point
│   └── styles.scss                  # Global styles
│
├── angular.json                     # Angular CLI workspace config
├── package.json                     # npm dependencies
├── tsconfig.json                    # TypeScript base config
├── tsconfig.app.json                # TypeScript app config
└── .prettierrc                      # Prettier formatting config
```

---

## Architecture Decisions

### Standalone Components

All components, pipes, and directives use `standalone: true`.
No NgModules are used. This is the Angular 17+ recommended approach.

### inject() Function

Constructor injection is replaced by the `inject()` function throughout.
This enables tree-shakable, testable, and composable dependency injection.

### OnPush Change Detection

All components use `ChangeDetectionStrategy.OnPush`.
This ensures change detection only triggers when inputs change or signals emit,
improving rendering performance.

### Signals

`AuthService` uses Angular signals (`signal<>`, `.asReadonly()`) for reactive state.
The architecture is signals-ready for future feature services.

### Functional Interceptors

HTTP interceptors use the functional `HttpInterceptorFn` pattern (Angular 15+).
Registered via `withInterceptors([...])` in `app.config.ts`.

### Lazy Loading

All routes use `loadComponent` or `loadChildren` for lazy loading.
Feature bundles are only loaded when the user navigates to that route.

### Path Aliases

TypeScript path aliases are configured in `tsconfig.json`:

```
@core/*   → src/app/core/*
@shared/* → src/app/shared/*
@features/* → src/app/features/*
@layouts/*  → src/app/layouts/*
@env/*      → src/environments/*
```

---

## Files Created

### Project Root
- `frontend/package.json`
- `frontend/angular.json`
- `frontend/tsconfig.json`
- `frontend/tsconfig.app.json`
- `frontend/.prettierrc`
- `frontend/.gitignore`

### Application Entry
- `frontend/src/index.html`
- `frontend/src/main.ts`
- `frontend/src/styles.scss`

### App Root
- `frontend/src/app/app.component.ts`
- `frontend/src/app/app.config.ts`
- `frontend/src/app/app.routes.ts`

### Core
- `frontend/src/app/core/services/api.service.ts`
- `frontend/src/app/core/services/auth.service.ts`
- `frontend/src/app/core/services/error-handler.service.ts`
- `frontend/src/app/core/interceptors/logging.interceptor.ts`
- `frontend/src/app/core/interceptors/error.interceptor.ts`
- `frontend/src/app/core/guards/auth.guard.ts`

### Shared
- `frontend/src/app/shared/components/spinner/spinner.component.ts`
- `frontend/src/app/shared/components/not-found/not-found.component.ts`
- `frontend/src/app/shared/pipes/truncate.pipe.ts`
- `frontend/src/app/shared/directives/click-outside.directive.ts`

### Features
- `frontend/src/app/features/dashboard/dashboard-placeholder.component.ts`

### Layouts
- `frontend/src/app/layouts/main-layout/main-layout.component.ts`

### Environments
- `frontend/src/environments/environment.ts`
- `frontend/src/environments/environment.production.ts`

---

## Files Modified

- `README.md` — added Frontend section
- `docs/commands/02-angular.md` — documented Angular CLI commands

---

## Commands Executed

None. All files written directly. `npm install` not executed in this milestone.

---

## Design Decisions

| Decision | Rationale |
|---|---|
| Standalone components | Angular 17+ standard; eliminates NgModules boilerplate |
| inject() | Cleaner than constructor injection; tree-shakable |
| OnPush everywhere | Better performance; forces explicit state management |
| Signals in AuthService | Future-ready reactive state without RxJS overhead |
| Functional interceptors | Angular 15+ standard; avoids class-based DI complexity |
| Lazy routes from day 1 | Prevents architectural debt; enforces bundle splitting |
| No mock data | Architecture milestone only; data comes in later milestones |

---

## Verification

After running `npm install` in `frontend/`:

```bash
cd frontend
npm install
npx ng build --configuration development
```

Expected: build succeeds, no TypeScript errors.

```bash
npx ng serve
```

Expected: app starts at `http://localhost:4200`, renders dashboard placeholder.

---

## Definition of Done

- [x] Angular project structure created
- [x] Standalone component architecture
- [x] Lazy-loading ready routing configured
- [x] Root AppComponent — router outlet only, no business logic
- [x] Core services: ApiService, AuthService, GlobalErrorHandlerService
- [x] Core interceptors: logging, error
- [x] Core guard: auth (placeholder)
- [x] Shared components: SpinnerComponent, NotFoundComponent
- [x] Shared pipe: TruncatePipe
- [x] Shared directive: ClickOutsideDirective
- [x] Features folder: dashboard placeholder
- [x] Layouts folder: main-layout placeholder
- [x] Environment files: development + production
- [x] TypeScript strict mode enabled
- [x] OnPush change detection throughout
- [x] inject() used throughout
- [x] Signals-ready architecture (AuthService)
- [x] README.md updated
- [x] docs/commands/02-angular.md updated
- [x] Milestone documentation created

---

## Next Milestone

**M2.0 — Application Layout**

Implement header, navigation, and footer inside `MainLayoutComponent`.
Wire layout routes to feature areas.
